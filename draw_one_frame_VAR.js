
var trail = []; // Array to store the previous positions of the rectangle

// Define the total number of rectangles to draw and remove
const totalRectangles = 40;

// Counter to keep track of how many rectangles have been drawn and removed
let drawnRectangles = 0;
let removedRectangles = 0;

let drawnCols = 0; // Track the number of columns drawn


var isFinish = false;
var start = false;


let circles = []; // Array to store information about the expanding circles
let color_switch = false;
let back_color_switch = false;



let stars = []; // Array to hold the exploding stars
let animationFinished = false; // Flag to track whether the animation is finished


let initialPositions = [];
let initialDiameters = [];

let showBlankCanvas = true;
let maxExpansionFrame = 23;
let backColor = 255;

//*************drawOneFrame 2 ****************** */

let test = 0;
let setupComplete = false;

let circles_2 = [];
let switch_02 = false;

