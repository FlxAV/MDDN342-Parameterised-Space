var startX = 0;
var startY = height / 2;
var endX = 0;
var endY = height / 2;
var lineWidth = 10;
var lineColor = 'black';
// this is the fireworks example
function draw_one_frame() {

	//Background
	fill(255, 255, 255);
  	rect(0, 0, width, height);

	//rect
	fill(0, 0, 0);
	rect(0, height/2, 50, 50);

}